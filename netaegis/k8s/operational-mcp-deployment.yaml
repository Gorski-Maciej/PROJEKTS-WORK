apiVersion: apps/v1
kind: Deployment
metadata:
  name: netaegis-operational-mcp
spec:
  replicas: 1
  selector:
    matchLabels: { app: netaegis-operational-mcp }
  template:
    metadata:
      labels: { app: netaegis-operational-mcp }
    spec:
      containers:
        - name: operational-mcp
          image: netaegis-operational:latest
          ports:
            - containerPort: 8001
